// Re-export from the photonics-dmx layer so existing main-side imports continue to work
// without each call site changing its path.
export { ChainFanout } from '../../photonics-dmx/controllers/ChainFanout'
